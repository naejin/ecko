{
  "name": "ecko",
  "description": "Deterministic code quality checks for AI agents -- echoes back mistakes at write-time so the agent self-corrects before the developer ever sees the code.",
  "version": "2.3.0",
  "mcpServers": {
    "ecko": {
      "command": "${CLAUDE_PLUGIN_ROOT}/scripts/run.sh",
      "args": ["--mode", "mcp-server"]
    }
  }
}
