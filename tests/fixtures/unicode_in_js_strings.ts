// Comment with em dash — should be skipped
/* Block comment with smart quotes “hello” */
const name = "“hello”";
const desc = 'it’s fine';
const tmpl = `template with — dash`;
/*
 * Multi-line block comment
 * with ‘smart quotes’
 */
const x = 1 + 2;
