# Expected: exit 0
# Em dash and smart quotes inside strings and comments are fine.
# This module handles text with special characters — like em dashes
msg = "Hello — world"
fancy = "Use \u201csmart quotes\u201d for display"
x = 1
